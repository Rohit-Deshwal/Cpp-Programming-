#include <iostream>
using namespace std; // here namespace std is used as default value because it removes every std:: from the code .
int main()
{
    // std::cout<<"hello world"; OR
    cout << "hello Rohit" << endl; // here the statement (endl) is like (\n)  a new line character prints a new line means hits Enter .
    cout << "Good Evening my Friend";

    return 0;
}